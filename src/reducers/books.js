import {
  CREATE_BOOK,
  GET_USER_BOOK_LIST,
  GET_BOOK_DETAIL,
  UPDATE_BOOK_DETAIL,
  DELETE_BOOK,
} from '../constants/ActionTypes';

let initialState = {
};

export default function restaurants(state = initialState, action) {
  return state;
};
